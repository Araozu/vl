[package]
name = "vl-semantic"
version = "0.1.0"
edition = "2021"
description = "VL name resolution: AST -> scopes"
license = "MIT OR Apache-2.0"

[dependencies]
vl-common = { path = "../vl-common" }
vl-syntax = { path = "../vl-syntax" }

[dev-dependencies]
vl-lex = { path = "../vl-lex" }

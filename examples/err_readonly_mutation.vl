type Counter = object {
    value: u64,
};

fun invalid(view: Counter, editable: *Counter) {
    view.value = 1;
    view = editable;
    val readonly: Counter = editable;
    val upgrade: *Counter = readonly;
}

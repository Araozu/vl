// Every source file is a module named after its filename.
use std.string;
use std.fs.{open, read};

function main() {
    let f = open("data.txt");
    let s = read(f);
    let n = string.len(s);
    n;
}

// Scalar literal suffixes and boolean conditional statements.
let count = 255u8;
let total = 1u64;
let delta = -1i64;
let ratio = 1.5f64;

function main(): void {
    if (true) {
        total;
    } else {
        count;
    }
    ratio;
}

// Contextual integer literals and boolean conditional statements.
let count = 255;
let total = 1;
let delta = -1;
let ratio = 1.5f64;

function main() {
    if (true) {
        total;
    } else {
        count;
    }
    ratio;
}

// Contextual integer literals and boolean conditional statements.
val count = 255;
val total = 1;
val delta = -1;
val ratio = 1.5f64;

fun main() {
    if (true) {
        total;
    } else {
        count;
    }
    ratio;
}
